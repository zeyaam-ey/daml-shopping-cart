# for config file options, refer to
# https://docs.daml.com/tools/assistant.html#project-config-file-daml-yaml

sdk-version: 2.5.0
name: DamlForgeApp
source: daml
init-script: Setup:setupUsers
version: 0.0.1
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
exposed-modules:
  - Main
